import { useEffect, useState, type FormEvent } from "react";
import { Link, useLocation } from "wouter";
import { CheckCircle2, Copy, Eye, EyeOff, GraduationCap, KeyRound, LogIn, RotateCcw, ShieldCheck, UserPlus, UserRound } from "lucide-react";
import { toast } from "sonner";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";

type Mode = "student-login" | "student-register" | "teacher" | "student-recovery" | "student-reset";
const inputClass = "mt-2 w-full rounded-xl border border-[#172b3f]/14 bg-white px-4 py-3 text-[#172b3f] outline-none transition focus:border-[#f05a3c] focus:ring-2 focus:ring-[#f05a3c]/15";

export default function SignInPage() {
  const [, setLocation] = useLocation();
  const { user, loading } = useAuth();
  const utils = trpc.useUtils();
  const [mode, setMode] = useState<Mode>("student-login");
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [displayName, setDisplayName] = useState("");
  const [recoveryCode, setRecoveryCode] = useState<string | null>(null);
  const [resetToken, setResetToken] = useState<string | null>(null);
  const [showPassword, setShowPassword] = useState(false);
  const [showConfirmPassword, setShowConfirmPassword] = useState(false);
  const isTeacher = mode === "teacher";
  const isRecovery = mode === "student-recovery" || mode === "student-reset";
  const resetVisibility = () => { setShowPassword(false); setShowConfirmPassword(false); };
  const afterLogin = async (destination: string) => { await utils.auth.me.invalidate(); setLocation(destination); };
  const studentLogin = trpc.auth.loginStudent.useMutation({ onSuccess: () => void afterLogin("/account"), onError: (error) => toast.error(error.message) });
  const studentRegister = trpc.auth.registerStudent.useMutation({ onSuccess: async (data) => { setRecoveryCode(data.recoveryCode); await utils.auth.me.invalidate(); }, onError: (error) => toast.error(error.message) });
  const teacherLogin = trpc.auth.loginTeacher.useMutation({ onSuccess: () => void afterLogin("/teacher"), onError: (error) => toast.error(error.message) });
  const requestReset = trpc.auth.requestStudentPasswordReset.useMutation({ onSuccess: (data) => { if (!data.resetToken) { toast.error("資料未能驗證。請檢查帳戶名稱與恢復碼，或使用已登入的裝置更新恢復碼。"); return; } setResetToken(data.resetToken); setPassword(""); setConfirmPassword(""); resetVisibility(); setMode("student-reset"); }, onError: () => toast.error("暫時未能開始重設，請稍後再試。") });
  const resetPassword = trpc.auth.resetStudentPassword.useMutation({ onSuccess: () => { setResetToken(null); setPassword(""); setConfirmPassword(""); resetVisibility(); setMode("student-login"); toast.success("密碼已重設，請用新密碼登入。原有學習進度已保留。"); }, onError: (error) => toast.error(error.message) });
  useEffect(() => { if (user && !recoveryCode) setLocation(user.role === "admin" ? "/teacher" : "/account"); }, [recoveryCode, setLocation, user]);
  const pending = studentLogin.isPending || studentRegister.isPending || teacherLogin.isPending || requestReset.isPending || resetPassword.isPending;
  const selectMode = (next: Extract<Mode, "student-login" | "student-register" | "teacher">) => { setMode(next); setPassword(""); setConfirmPassword(""); resetVisibility(); if (next !== "student-register") setDisplayName(""); if (next === "teacher" && !username) setUsername("admin"); };
  const submit = (event: FormEvent) => { event.preventDefault(); if (mode === "teacher") teacherLogin.mutate({ username, password }); else if (mode === "student-register") studentRegister.mutate({ username, password, displayName }); else if (mode === "student-recovery") requestReset.mutate({ username, recoveryCode: password }); else if (mode === "student-reset") { if (!resetToken) { setMode("student-recovery"); return; } if (password !== confirmPassword) { toast.error("兩次輸入的新密碼不一致。"); return; } resetPassword.mutate({ resetToken, newPassword: password }); } else studentLogin.mutate({ username, password }); };
  if (loading) return <main className="grid min-h-screen place-items-center bg-[#f8f5ed] font-mono font-bold text-[#172b3f]">正在準備登入…</main>;
  const heading = isTeacher ? "教師登入" : recoveryCode ? "保存你的恢復碼" : mode === "student-register" ? "建立學生帳戶" : mode === "student-recovery" ? "忘記密碼" : mode === "student-reset" ? "設定新密碼" : "歡迎回來";
  return <main className={`min-h-screen px-5 py-8 sm:p-10 ${isTeacher ? "bg-[#172b3f] text-white" : "bg-[#f8f5ed] text-[#172b3f]"}`}><div className="mx-auto grid min-h-[calc(100vh-4rem)] max-w-5xl overflow-hidden rounded-[32px] border border-white/10 bg-white shadow-[12px_12px_0_rgba(23,43,63,.12)] md:grid-cols-[.88fr_1.12fr]"><aside className={`p-7 sm:p-10 ${isTeacher ? "bg-[#f6be5d] text-[#172b3f]" : "bg-[#172b3f] text-white"}`}><Link href="/" className="font-bold hover:opacity-70">← 返回學習地圖</Link><div className="mt-14"><span className={`grid size-14 place-items-center rounded-2xl ${isTeacher ? "bg-[#172b3f] text-[#f6be5d]" : "bg-[#f05a3c] text-white"}`}>{isTeacher ? <GraduationCap className="size-7" /> : isRecovery ? <RotateCcw className="size-7" /> : <UserRound className="size-7" />}</span><p className={`mt-7 font-mono text-[11px] font-bold tracking-[.17em] ${isTeacher ? "text-[#6b4b10]" : "text-[#80d8cf]"}`}>{isTeacher ? "TEACHER CONSOLE" : isRecovery ? "SAFE ACCOUNT RECOVERY" : "STUDENT LEARNING"}</p><h1 className="mt-3 text-4xl font-black leading-tight">{isTeacher ? "教師專屬管理" : isRecovery ? "安全取回帳戶" : "用自己的帳戶學習"}</h1><p className={`mt-5 max-w-sm leading-7 ${isTeacher ? "text-[#533e15]" : "text-white/72"}`}>{isTeacher ? "登入後可檢視已註冊學生的暱稱、帳戶名稱、同步時間與學習進度。" : isRecovery ? "使用註冊時保存的恢復碼確認身分。重設密碼不會刪除完成星星、錯題或每日紀錄。" : "學生不用 Google 帳戶。註冊後，完成星星、錯題與每日目標會安全備份。"}</p></div></aside><section className="p-7 sm:p-10"><div className="flex rounded-2xl bg-[#f3f0e8] p-1"><button type="button" onClick={() => selectMode("student-login")} className={`flex-1 rounded-xl px-3 py-2 text-sm font-bold ${mode === "student-login" ? "bg-white text-[#172b3f] shadow-sm" : "text-[#617286]"}`}>學生登入</button><button type="button" onClick={() => selectMode("student-register")} className={`flex-1 rounded-xl px-3 py-2 text-sm font-bold ${mode === "student-register" ? "bg-white text-[#172b3f] shadow-sm" : "text-[#617286]"}`}>學生註冊</button><button type="button" onClick={() => selectMode("teacher")} className={`flex-1 rounded-xl px-3 py-2 text-sm font-bold ${mode === "teacher" ? "bg-[#172b3f] text-white shadow-sm" : "text-[#617286]"}`}>教師</button></div><div className="mt-9"><p className="font-mono text-[11px] font-bold tracking-[.15em] text-[#f05a3c]">{isTeacher ? "TEACHER SIGN IN" : recoveryCode ? "ONE-TIME RECOVERY CODE" : isRecovery ? "STUDENT ACCOUNT RECOVERY" : mode === "student-register" ? "CREATE STUDENT ACCOUNT" : "STUDENT SIGN IN"}</p><h2 className="mt-3 text-3xl font-black">{heading}</h2><p className="mt-3 text-sm leading-6 text-[#617286]">{isTeacher ? "教師帳戶名稱為 admin。學生帳戶不可開啟教師管理頁。" : recoveryCode ? "這組恢復碼只會顯示一次。請立即安全保存，切勿分享給任何人。" : mode === "student-recovery" ? "輸入用戶名稱與你保存的恢復碼，系統會建立一個 15 分鐘內有效、只可使用一次的重設步驟。" : mode === "student-reset" ? "新密碼設定後，所有舊登入會失效；你的學習進度會保留在同一帳戶。" : mode === "student-register" ? "用戶名稱只可使用英文小寫字母、數字與 . _ -；密碼最少 6 個字元。" : "輸入你的學生帳戶，即可取回同步的學習紀錄。"}</p></div>{recoveryCode ? <RecoveryCode code={recoveryCode} onContinue={() => { setRecoveryCode(null); setLocation("/account"); }} /> : <form onSubmit={submit} className="mt-7 grid gap-4">{mode === "student-register" && <label><span className="text-xs font-bold text-[#617286]">學生暱稱</span><input required value={displayName} onChange={(event) => setDisplayName(event.target.value)} maxLength={80} placeholder="例如：數學小探險家" className={inputClass} /></label>}{mode !== "student-reset" && <label><span className="text-xs font-bold text-[#617286]">用戶名稱</span><input required autoComplete="username" value={username} onChange={(event) => setUsername(event.target.value.toLowerCase())} placeholder={isTeacher ? "admin" : "例如：maths.chan"} className={`${inputClass} font-mono`} /></label>}<LoginSecretField label={mode === "student-recovery" ? "恢復碼" : mode === "student-reset" ? "新密碼" : "密碼"} value={password} onChange={setPassword} visible={showPassword} onToggle={() => setShowPassword((value) => !value)} revealable={mode !== "student-recovery"} autoComplete={mode === "student-register" || mode === "student-reset" ? "new-password" : mode === "student-recovery" ? "one-time-code" : "current-password"} minLength={mode === "student-register" || mode === "student-reset" ? 6 : 1} placeholder={mode === "student-recovery" ? "例如：ABCD-EF01-2345-6789" : "輸入密碼"} />{mode === "student-reset" && <LoginSecretField label="確認新密碼" value={confirmPassword} onChange={setConfirmPassword} visible={showConfirmPassword} onToggle={() => setShowConfirmPassword((value) => !value)} autoComplete="new-password" minLength={6} placeholder="再次輸入新密碼" /> }<button disabled={pending} className={`mt-2 inline-flex items-center justify-center gap-2 rounded-xl px-5 py-3 font-bold text-white disabled:opacity-60 ${isTeacher ? "bg-[#172b3f]" : "bg-[#f05a3c]"}`}>{isTeacher ? <ShieldCheck className="size-4" /> : mode === "student-register" ? <UserPlus className="size-4" /> : mode === "student-recovery" ? <KeyRound className="size-4" /> : mode === "student-reset" ? <RotateCcw className="size-4" /> : <LogIn className="size-4" />}{pending ? "正在處理…" : isTeacher ? "登入教師管理頁" : mode === "student-register" ? "建立學生帳戶" : mode === "student-recovery" ? "驗證並繼續" : mode === "student-reset" ? "重設我的密碼" : "登入學生帳戶"}</button></form>}{mode === "student-login" && !recoveryCode && <button type="button" onClick={() => { setMode("student-recovery"); setPassword(""); resetVisibility(); }} className="mt-5 text-sm font-bold text-[#0e8b87] underline decoration-[#0e8b87]/40 underline-offset-4">忘記密碼？使用恢復碼重設</button>}{isRecovery && !recoveryCode && <button type="button" onClick={() => selectMode("student-login")} className="mt-5 text-sm font-bold text-[#0e8b87] underline decoration-[#0e8b87]/40 underline-offset-4">返回學生登入</button>}<p className="mt-6 flex items-start gap-2 rounded-xl bg-[#f8f5ed] p-3 text-xs leading-5 text-[#617286]"><KeyRound className="mt-0.5 size-4 shrink-0 text-[#0e8b87]" />帳密及恢復碼只會在伺服器端以安全雜湊方式驗證；密碼不會被寫入前端或公開程式碼。</p></section></div></main>;
}

function LoginSecretField({ label, value, onChange, visible, onToggle, revealable = true, autoComplete, minLength, placeholder }: { label: string; value: string; onChange: (value: string) => void; visible: boolean; onToggle: () => void; revealable?: boolean; autoComplete: string; minLength: number; placeholder: string }) { return <label><span className="text-xs font-bold text-[#617286]">{label}</span><span className="relative mt-2 block"><input required autoComplete={autoComplete} type={revealable && visible ? "text" : revealable ? "password" : "text"} value={value} onChange={(event) => onChange(event.target.value)} minLength={minLength} maxLength={128} placeholder={placeholder} className={`${inputClass} pr-24`} style={{ color: "#172b3f", WebkitTextFillColor: "#172b3f" }} />{revealable && <button type="button" onClick={onToggle} aria-label={`${visible ? "隱藏" : "顯示"}${label}`} aria-pressed={visible} className="absolute inset-y-0 right-11 grid w-10 place-items-center rounded-xl text-[#617286] hover:text-[#172b3f] focus:outline-none focus-visible:ring-2 focus-visible:ring-[#f05a3c]">{visible ? <EyeOff className="size-5" aria-hidden="true" /> : <Eye className="size-5" aria-hidden="true" />}</button>}</span></label>; }
function RecoveryCode({ code, onContinue }: { code: string; onContinue: () => void }) { return <div className="mt-7 rounded-2xl border border-[#0e8b87]/30 bg-[#e8f5f2] p-5"><div className="flex items-start gap-3"><CheckCircle2 className="mt-0.5 size-5 shrink-0 text-[#0e8b87]" /><div><strong>帳戶已建立。</strong><p className="mt-1 text-sm leading-6 text-[#135d59]">用它來重設密碼。關閉此頁後，系統不會再次顯示這組代碼。</p></div></div><div className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-xl bg-white px-4 py-3"><code className="select-all text-lg font-black tracking-[.15em] text-[#172b3f]">{code}</code><button type="button" onClick={() => void navigator.clipboard.writeText(code).then(() => toast.success("恢復碼已複製。"))} className="inline-flex items-center gap-2 rounded-lg border border-[#172b3f]/15 px-3 py-2 text-xs font-bold"><Copy className="size-3.5" />複製</button></div><button type="button" onClick={onContinue} className="mt-5 w-full rounded-xl bg-[#172b3f] px-5 py-3 font-bold text-white">我已安全保存，前往我的帳戶</button></div>; }
