// @ts-nocheck
import{useState}from"react";import{Link}from"wouter";const routes=["前進 → 右轉 → 前進","前進 → 左轉 → 前進","前進 → 前進 → 右轉","右轉 → 前進 → 左轉","左轉 → 前進 → 前進","前進 → 右轉 → 左轉","右轉 → 前進 → 前進","前進 → 左轉 → 右轉"],bars=[{a:4,b:2,k:5},{a:3,b:5,k:2},{a:6,b:4,k:2},{a:2,b:5,k:5},{a:5,b:3,k:2},{a:4,b:6,k:5},{a:3,b:2,k:10},{a:5,b:4,k:2}];export default function P4AdvancedDataPractice(){const mode=location.pathname.includes("route")?"r":"b",data=mode==="r"?routes:bars;const[i,setI]=useState(0),[ok,setOk]=useState(false);const q=data[i],answer=mode==="r"?q:Math.abs(q.a-q.b)*q.k;return <main className="min-h-screen bg-[#f8f5ed] p-6 text-[#172b3f]"><Link href="/#path">← 返回題目庫</Link><p className="mt-8 font-mono text-[#6c8b4c]">P4 · 8 題進階站</p><h1 className="text-4xl font-black">{mode==="r"?"八方向路線規劃":"棒形圖兩組資料比較"}</h1><section className="mq-practice-card mt-6 rounded-3xl border bg-white p-8 text-center"><p>第 {i+1}/8 題</p>{mode==="r"?<><div className="my-7 text-7xl">🧭</div><h2>選出可到達目的地的指令路線</h2><div className="mq-concept-choices mx-auto mt-7">{[q,"前進 → 後退","左轉 → 前進","右轉 → 後退"].map(x=><button key={x} onClick={()=>setOk(x===q)}>{x}</button>)}</div></>:<><h2>甲有 {q.a} 格，乙有 {q.b} 格，每格代表 {q.k} 人。</h2><div className="my-7 flex h-48 items-end justify-center gap-6"><i className="w-14 bg-[#6c8b4c]" style={{height:q.a*20}}/><i className="w-14 bg-[#f6be5d]" style={{height:q.b*20}}/></div><p>兩組相差多少人？</p><div className="mq-concept-choices mx-auto mt-6">{[answer,answer+q.k,Math.max(1,answer-q.k),answer+2].map(x=><button key={x} onClick={()=>setOk(x===answer)}>{x}</button>)}</div></>}{ok&&<button className="mq-start mt-6 rounded-xl bg-[#f05a3c] px-5 py-3 text-white" onClick={()=>{setOk(false);setI(i===7?0:i+1)}}>答對了！下一題</button>}</section></main>}
