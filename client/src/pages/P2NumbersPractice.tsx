// @ts-nocheck
/** Maths Quest P2 三位數：以芥黃色數位值方格連結百、十、個位，維持手帳式八題探險節奏。 */
import { useEffect, useState } from "react";
import { Link } from "wouter";
import { ArrowLeft, ArrowRight, Check, Lightbulb, Moon, RotateCcw, Star, Sun, Trophy, Volume2, VolumeX, X } from "lucide-react";
import { useTheme } from "@/contexts/ThemeContext";
import { playCelebrationSound, playCorrectSound, playWrongSound } from "@/lib/sounds";
import { markPracticeCompleted } from "@/lib/practiceCompletion";
import { recordDailyPractice } from "@/lib/dailyPractice";
import { speakCorrectEncouragement, speakTryAgain } from "@/lib/speech";
import SpeakButton from "@/components/SpeakButton";
import CorrectCelebration from "@/components/CorrectCelebration";
import MidpointBreak from "@/components/MidpointBreak";
import FinishAchievementOverlay from "@/components/FinishAchievementOverlay";

const questions = [
  { number: 245, ask: "哪一個數是由 2 個百、4 個十和 5 個一組成？", answer: 245, choices: [245, 425, 254] },
  { number: 361, ask: "3 在哪一個位？", answer: "百位", choices: ["百位", "十位", "個位"] },
  { number: 508, ask: "哪一個數是由 5 個百、0 個十和 8 個一組成？", answer: 508, choices: [580, 508, 805] },
  { number: 472, ask: "7 在哪一個位？", answer: "十位", choices: ["百位", "十位", "個位"] },
  { number: 129, ask: "哪一個數是由 1 個百、2 個十和 9 個一組成？", answer: 129, choices: [192, 219, 129] },
  { number: 634, ask: "4 在哪一個位？", answer: "個位", choices: ["百位", "十位", "個位"] },
  { number: 810, ask: "哪一個數是由 8 個百、1 個十和 0 個一組成？", answer: 810, choices: [801, 810, 180] },
  { number: 293, ask: "9 在哪一個位？", answer: "十位", choices: ["百位", "十位", "個位"] },
];

export default function P2NumbersPractice() {
  const { theme, toggleTheme } = useTheme();
  const [index, setIndex] = useState(0); const [result, setResult] = useState<"idle" | "correct" | "incorrect">("idle"); const [score, setScore] = useState(0); const [seconds, setSeconds] = useState(0); const [sound, setSound] = useState(true); const [finished, setFinished] = useState(false); const [midpointOpen, setMidpointOpen] = useState(false); const [wrongItems, setWrongItems] = useState([]);
  const question = questions[index]; const complete = index + (result === "correct" ? 1 : 0); const stars = score === 8 ? 3 : score >= 6 ? 2 : score ? 1 : 0; const time = `${Math.floor(seconds / 60)}:${String(seconds % 60).padStart(2, "0")}`; const digits = String(question.number).padStart(3, "0").split("");
  useEffect(() => { if (finished) return; const timer = window.setInterval(() => setSeconds((value) => value + 1), 1000); return () => window.clearInterval(timer); }, [finished]);
  const choose = (choice: string | number) => { if (result !== "idle") return; if (choice === question.answer) { setResult("correct"); setScore((value) => value + 1); if (sound) { playCorrectSound(); speakCorrectEncouragement(); } } else { setResult("incorrect"); setWrongItems((items) => [...items, { prompt: question.ask, answer: String(question.answer), hint: `看清楚百、十、個位。` }]); if (sound) { playWrongSound(); speakTryAgain(); } } };
  const next = () => { if (index === 7) { setFinished(true); markPracticeCompleted("p2-numbers"); recordDailyPractice("p2-numbers"); if (sound) playCelebrationSound(); return; } if (index === 3) setMidpointOpen(true); setIndex((value) => value + 1); setResult("idle"); };
  const restart = () => { setIndex(0); setResult("idle"); setScore(0); setSeconds(0); setFinished(false); setMidpointOpen(false); setWrongItems([]); };
  return <div className="mq-practice mq-p2-practice mq-p2-numbers min-h-screen bg-[#f8f5ed] text-[#172b3f] dark:bg-[#101b27] dark:text-[#f4f7f4]"><MidpointBreak open={midpointOpen} onContinue={() => setMidpointOpen(false)} /><header className="mq-practice-header sticky top-0 z-50 border-b border-[#172b3f]/10 bg-[#f8f5ed]/92 backdrop-blur-xl dark:border-white/10 dark:bg-[#111c28]/92"><div className="mx-auto flex h-[72px] max-w-[1280px] items-center justify-between px-5 lg:px-8"><Link href="/" className="group flex items-center gap-3"><span className="grid size-10 place-items-center rounded-[14px] bg-[#f05a3c] shadow-[0_6px_0_#c84932]"><img src="/manus-storage/maths-quest-logo_cf7e4a6a.png" alt="" className="size-6 brightness-0 invert" /></span><span className="leading-none"><strong className="block text-[16px] font-extrabold">Maths Quest</strong><small className="mt-1 block font-mono text-[9px] font-bold tracking-[.14em] text-[#c8811e]">P2.02 · 互動站</small></span></Link><div className="flex items-center gap-2 sm:gap-4"><button onClick={() => setSound((value) => !value)} className="mq-theme-switch grid size-10 place-items-center rounded-full border border-[#172b3f]/15 bg-white/70 dark:bg-white/10" aria-label="切換語音">{sound ? <Volume2 className="size-[17px]" /> : <VolumeX className="size-[17px]" />}</button><button onClick={toggleTheme} className="mq-theme-switch grid size-10 place-items-center rounded-full border border-[#172b3f]/15 bg-white/70 dark:bg-white/10" aria-label="切換深色模式">{theme === "light" ? <Moon className="size-[17px]" /> : <Sun className="size-[18px]" />}</button><Link href="/#path" className="mq-library-link inline-flex items-center gap-2 rounded-full border border-[#172b3f]/15 px-3 py-2 text-sm font-extrabold"><ArrowLeft className="size-4" /><span>返回題目庫</span></Link></div></div></header><main className="mx-auto max-w-[1280px] px-5 py-8 lg:px-8 lg:py-10"><div className="mq-route-ruler"><span>起點</span><i /><span>P2.02</span><i /><span>互動站</span></div><div className="mb-6 flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="font-mono text-[11px] font-bold tracking-[.16em] text-[#c8811e]">P2 · 數字與位值</p><h1 className="mt-2 text-3xl font-black tracking-[-.055em] sm:text-4xl">三位數</h1><p className="mt-2 text-sm leading-6 text-[#617286] dark:text-[#b7c8ce]">用百位、十位、個位看清楚三位數。</p></div><div className="mq-progress-label rounded-2xl border border-[#172b3f]/10 bg-white px-4 py-3 text-sm dark:bg-[#1c3041]"><span className="font-mono text-[10px] font-bold tracking-[.12em] text-[#c8811e]">數字任務</span><p className="mt-1 font-extrabold">完成 {complete} / 8 題 · {time}</p></div></div><div className="mq-shape-controls"><div><span className="mq-station-stamp">PLACE VALUE LAB</span><strong>看百、十、個位</strong><small>先看每個數字的位置，再選答案。</small></div><span className="mq-p2-station-icon">100</span></div><div className="mq-practice-grid mt-5 grid gap-5 lg:grid-cols-[230px_minmax(0,1fr)]"><aside className="mq-practice-sidebar order-2 lg:order-1"><div className="rounded-[24px] border border-[#172b3f]/10 bg-[#172b3f] p-5 text-white shadow-[0_10px_0_#0e1d2a]"><p className="font-mono text-[10px] font-bold tracking-[.16em] text-[#f6be5d]">數字任務</p><h2 className="mt-3 text-xl font-black leading-7">先分位，再讀數。</h2><div className="mq-mission-steps mt-5">{Array.from({ length: 8 }, (_, number) => <span key={number} className={number < complete ? "mq-mission-step-done" : number === index ? "mq-mission-step-current" : ""}>{number + 1}</span>)}</div><div className="mt-6 h-2 overflow-hidden rounded-full bg-white/15"><div className="h-full rounded-full bg-[#c8811e] transition-[width] duration-500" style={{ width: `${complete / 8 * 100}%` }} /></div><div className="mq-stars mt-5">{[1, 2, 3].map((star) => <Star key={star} className={`size-5 ${star <= stars ? "fill-[#f6be5d] text-[#f6be5d]" : "text-white/20"}`} />)}</div></div><div className="mq-tip mt-5 rounded-2xl border border-[#172b3f]/10 bg-[#fff7e9] p-4"><div className="flex items-center gap-2 text-[#c8811e]"><Lightbulb className="size-4" /><span className="font-mono text-[10px] font-bold tracking-[.12em]">小提示</span></div><p className="mt-2 text-sm font-bold leading-6 text-[#744230]">最左是百位，中間是十位，最右是個位。</p></div></aside><section className="mq-practice-card mq-p2-card order-1 min-h-[560px] overflow-hidden rounded-[28px] border border-[#172b3f]/10 bg-white p-5 shadow-[0_16px_35px_rgba(23,43,63,.07)] dark:bg-[#1c3041] md:p-8">{result === "correct" && <CorrectCelebration />}{!finished ? <><div className="mq-question-head flex items-center justify-between border-b border-[#172b3f]/10 pb-5 dark:border-white/10"><div><p className="font-mono text-[10px] font-bold tracking-[.14em] text-[#c8811e]">數字站 · 第 {index + 1} 題</p><p className="mt-1 text-sm font-bold text-[#617286]">看位值，再選答案。</p></div><span className="grid size-10 place-items-center rounded-full bg-[#fff7e9] font-mono text-xs font-black text-[#c8811e]">{index + 1}</span></div><div className="flex min-h-[345px] flex-col items-center justify-center py-7 text-center"><div className="mq-question-note"><span className="font-mono">位值提示</span><span>百 · 十 · 個</span></div><SpeakButton text={`${question.ask} 數字是 ${question.number}。`} /><h2 className="mt-3 text-[clamp(1.5rem,4vw,2.35rem)] font-black tracking-[-.05em]">{question.ask}</h2><div className="mq-place-board"><div><small>百位</small><strong>{digits[0]}</strong><i>{Array.from({ length: Number(digits[0]) }, (_, n) => <span key={n} />)}</i></div><div><small>十位</small><strong>{digits[1]}</strong><i>{Array.from({ length: Number(digits[1]) }, (_, n) => <span key={n} />)}</i></div><div><small>個位</small><strong>{digits[2]}</strong><i>{Array.from({ length: Number(digits[2]) }, (_, n) => <span key={n} />)}</i></div></div></div>{result !== "idle" && <div className={`mq-feedback mb-5 flex items-start gap-3 rounded-2xl border p-4 ${result === "correct" ? "border-[#0e8b87]/25 bg-[#e8f5f2] text-[#135d59]" : "border-[#f05a3c]/25 bg-[#fff0e9] text-[#8c3f2e]"}`}><span className={`grid size-8 shrink-0 place-items-center rounded-full ${result === "correct" ? "bg-[#0e8b87]" : "bg-[#f05a3c]"} text-white`}>{result === "correct" ? <Check className="size-4" /> : <X className="size-4" />}</span><div><p className="font-extrabold">{result === "correct" ? "答對了！你看清楚位值了。" : "再試一次，先看百、十、個位。"}</p></div></div>}<div className="grid gap-3 sm:grid-cols-[1fr_180px]"><div className="grid grid-cols-3 gap-3">{question.choices.map((choice) => <button key={choice} onClick={() => choose(choice)} disabled={result !== "idle"} className="mq-place-choice">{choice}</button>)}</div><div className="flex flex-col justify-end">{result === "idle" ? <p className="rounded-2xl border border-dashed border-[#172b3f]/15 p-4 text-center text-sm font-bold text-[#617286]">選一個答案。</p> : result === "incorrect" ? <button onClick={() => setResult("idle")} className="mq-start min-h-14 rounded-2xl bg-[#f05a3c] px-5 text-sm font-extrabold text-white"><RotateCcw className="mr-1 inline size-4" /> 再試一次</button> : <button onClick={next} className="mq-start min-h-14 rounded-2xl bg-[#c8811e] px-5 text-sm font-extrabold text-white">{index === 7 ? "看結果" : "下一題"} <ArrowRight className="ml-1 inline size-4" /></button>}</div></div></> : <div className="flex min-h-[500px] flex-col items-center justify-center text-center"><span className="grid size-20 place-items-center rounded-[26px] bg-[#fff7e9] text-[#c8811e]"><Trophy className="size-10" /></span><p className="mt-7 font-mono text-[11px] font-bold tracking-[.16em] text-[#c8811e]">完成數字站</p><h2 className="mt-3 text-3xl font-black tracking-[-.05em]">三位數完成了！</h2><div className="mq-finish-stars mt-4">{[1, 2, 3].map((star) => <Star key={star} className={`size-7 ${star <= stars ? "fill-[#f6be5d] text-[#f6be5d]" : "text-[#e8e3d9]"}`} />)}</div><p className="mt-3 text-[15px] leading-7 text-[#617286]">答對 {score} / 8 題。</p><FinishAchievementOverlay perfect={score === 8} items={wrongItems} /><div className="mt-7 flex flex-wrap justify-center gap-3"><button onClick={restart} className="mq-start inline-flex items-center gap-2 rounded-full bg-[#c8811e] px-5 py-3 text-sm font-extrabold text-white"><RotateCcw className="size-4" /> 再做一次</button><Link href="/#path" className="mq-library-return inline-flex items-center gap-2 rounded-full border border-[#172b3f]/15 px-5 py-3 text-sm font-extrabold">返回題目庫 <ArrowRight className="size-4" /></Link></div></div>}</section></div></main></div>;
}
