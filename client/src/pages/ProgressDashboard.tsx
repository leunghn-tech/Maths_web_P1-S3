import{Link}from"wouter";import{getCompletedPractices}from"@/lib/practiceCompletion";const totals={P1:14,P2:19,P3:18,P4:6,P5:4,P6:2};export default function ProgressDashboard(){const done=getCompletedPractices();const recent=[...done].slice(-8).reverse();return <main className="min-h-screen bg-[#f8f5ed] p-6 text-[#172b3f]"><Link href="/">← 返回首頁</Link><p className="mt-8 font-mono text-[#f05a3c]">FAMILY DASHBOARD</p><h1 className="text-4xl font-black">家長／教師進度總覽</h1><section className="mq-practice-card mt-6 rounded-3xl border bg-white p-8"><h2 className="text-2xl font-black">各年級完成進度</h2><div className="mt-5 grid gap-4 md:grid-cols-2">{Object.entries(totals).map(([g,t])=>{const n=done.filter(x=>x.startsWith(g.toLowerCase()+"-")).length,p=Math.min(100,Math.round(n/t*100));return <div key={g} className="rounded-2xl border bg-[#fffaf0] p-4"><b>{g}：{n}/{t} 站 · {p}%</b><div className="mt-3 h-3 overflow-hidden rounded-full bg-[#e6dfcf]"><i className="block h-full bg-[#f05a3c]" style={{width:`${p}%`}}/></div></div>})}</div><h2 className="mt-8 text-2xl font-black">最近練習紀錄</h2><p className="mt-3">{recent.join("、")||"尚未完成練習"}</p><h2 className="mt-8 text-2xl font-black">個人化複習清單</h2><p className="mt-3">優先重做未完成或尚未拿到星星的 P3 站點；完成後下一顆星提示會自動更新。</p><Link href="/#path" className="mq-start mt-5 inline-block rounded-xl bg-[#f05a3c] px-5 py-3 text-white">前往題目庫</Link></section></main>}
