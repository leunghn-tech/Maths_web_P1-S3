/** Maths Quest P1 空間遊戲：生活物件分類、圖形旋轉與長度排序。 */
import { useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { ArrowLeft, ArrowRight, Check, Cuboid, Lightbulb, Moon, RotateCcw, Sparkles, Star, Sun, Trophy, Volume2, VolumeX, X } from "lucide-react";
import { useTheme } from "@/contexts/ThemeContext";
import { playCelebrationSound, playCorrectSound, playWrongSound } from "@/lib/sounds";
import { markPracticeCompleted } from "@/lib/practiceCompletion";
import { recordDailyPractice } from "@/lib/dailyPractice";
import { speakCantonese, speakCorrectEncouragement, speakTryAgain } from "@/lib/speech";
import SpeakButton from "@/components/SpeakButton";
import CorrectCelebration from "@/components/CorrectCelebration";

type Mode = "solid" | "rotate" | "sort"; type Result = "idle" | "correct" | "incorrect";
type Solid = "球體" | "柱體" | "錐體";
const solidItems = [{ emoji: "⚽", name: "足球", answer: "球體" as Solid }, { emoji: "🥫", name: "汽水罐", answer: "柱體" as Solid }, { emoji: "🍦", name: "雪糕筒", answer: "錐體" as Solid }, { emoji: "🪀", name: "悠悠球", answer: "球體" as Solid }, { emoji: "🕯️", name: "蠟燭", answer: "柱體" as Solid }, { emoji: "🎉", name: "派對帽", answer: "錐體" as Solid }];
const rotationItems = [{ shape: "▲", name: "三角形", degree: 120 }, { shape: "▰", name: "長方形", degree: 90 }, { shape: "◆", name: "正方形", degree: 45 }, { shape: "▶", name: "三角形", degree: 90 }];
const sortItems = [[{ emoji: "✏️", name: "鉛筆", length: 7 }, { emoji: "📏", name: "尺子", length: 14 }, { emoji: "🎀", name: "絲帶", length: 20 }], [{ emoji: "🥄", name: "湯匙", length: 9 }, { emoji: "🪥", name: "牙刷", length: 12 }, { emoji: "🌿", name: "樹枝", length: 17 }], [{ emoji: "🧱", name: "積木", length: 5 }, { emoji: "🥤", name: "吸管", length: 15 }, { emoji: "🪢", name: "繩子", length: 22 }]];
const shuffle = <T,>(items: T[]) => [...items].sort(() => Math.random() - .5);
const modeFromPath = (path: string): Mode => path.includes("rotation") ? "rotate" : path.includes("length-sort") ? "sort" : "solid";
const meta: Record<Mode, { key: string; code: string; title: string; subtitle: string; action: string; hint: string; icon: string }> = { solid: { key: "p1-solids", code: "P1.07", title: "立體圖形", subtitle: "把生活物件分類到正確立體形狀。", action: "選出它是哪一種立體形狀", hint: "圓圓會滾動的是球體；上下平平的是柱體；尖尖一端的是錐體。", icon: "🧊" }, rotate: { key: "p1-shape-rotation", code: "P1.06B", title: "旋轉圖形", subtitle: "轉一轉，圖形還是同一個圖形。", action: "把圖形轉到指定方向", hint: "轉動只是改變方向，邊和角的數量不會改變。", icon: "🔄" }, sort: { key: "p1-length-sort", code: "P1.05C", title: "長度排序", subtitle: "把三件物件由短到長排好。", action: "按由短到長的次序點選物件", hint: "先找最短，再找中間，最後就是最長。", icon: "📏" } };

export default function P1SpatialPractice() {
  const [location] = useLocation(); const mode = modeFromPath(location); const info = meta[mode]; const { theme, toggleTheme } = useTheme();
  const session = useMemo(() => Array.from({ length: 8 }, (_, i) => mode === "solid" ? solidItems[i % solidItems.length] : mode === "rotate" ? rotationItems[i % rotationItems.length] : sortItems[i % sortItems.length]), [mode]);
  const [index, setIndex] = useState(0); const [result, setResult] = useState<Result>("idle"); const [score, setScore] = useState(0); const [seconds, setSeconds] = useState(0); const [sound, setSound] = useState(true); const [finished, setFinished] = useState(false); const [selected, setSelected] = useState<string[]>([]); const [rotation, setRotation] = useState(0); const item = session[index];
  const completed = index + (result === "correct" ? 1 : 0); const stars = score === 8 ? 3 : score >= 6 ? 2 : score ? 1 : 0; const timeLabel = `${Math.floor(seconds / 60)}:${String(seconds % 60).padStart(2, "0")}`;
  const resetQuestion = () => { setResult("idle"); setSelected([]); setRotation(0); };
  const verify = (answer?: string) => { if (result !== "idle") return; let correct = false; if (mode === "solid") correct = answer === (item as typeof solidItems[number]).answer; else if (mode === "rotate") correct = rotation % 360 === (item as typeof rotationItems[number]).degree; else { const expected = [...(item as typeof sortItems[number])].sort((a, b) => a.length - b.length).map((entry) => entry.name); correct = selected.length === 3 && selected.every((name, i) => name === expected[i]); } if (correct) { setResult("correct"); setScore((v) => v + 1); if (sound) { playCorrectSound(); speakCorrectEncouragement(); } } else { setResult("incorrect"); if (sound) { playWrongSound(); speakTryAgain(); } } };
  const next = () => { if (index === 7) { setFinished(true); markPracticeCompleted(info.key); recordDailyPractice(info.key); if (sound) playCelebrationSound(); return; } setIndex((v) => v + 1); resetQuestion(); };
  const restart = () => { setIndex(0); setResult("idle"); setScore(0); setSeconds(0); setFinished(false); setSelected([]); setRotation(0); };
  const prompt = mode === "solid" ? `看看 ${ (item as typeof solidItems[number]).name }，它是球體、柱體還是錐體？` : mode === "rotate" ? `把 ${ (item as typeof rotationItems[number]).name } 轉到指定方向。` : "請把三件物件由短到長排列。";
  return <div className={`mq-practice mq-p1-spatial mq-p1-${mode} min-h-screen bg-[#f8f5ed] text-[#172b3f] dark:bg-[#101b27] dark:text-[#f4f7f4]`}><header className="mq-practice-header sticky top-0 z-50 border-b border-[#172b3f]/10 bg-[#f8f5ed]/92 backdrop-blur-xl"><div className="mx-auto flex h-[72px] max-w-[1280px] items-center justify-between px-5 lg:px-8"><Link href="/" className="group flex items-center gap-3"><span className="grid size-10 place-items-center rounded-[14px] bg-[#f05a3c] shadow-[0_6px_0_#c84932]"><img src="/manus-storage/maths-quest-logo_cf7e4a6a.png" alt="" className="size-6 brightness-0 invert" /></span><span className="leading-none"><strong className="block text-[16px] font-extrabold">Maths Quest</strong><small className="mt-1 block font-mono text-[9px] font-bold tracking-[.14em] text-[#f05a3c]">{info.code} · 互動站</small></span></Link><div className="flex items-center gap-2 sm:gap-4"><button onClick={() => setSound((v) => !v)} className="mq-theme-switch grid size-10 place-items-center rounded-full border border-[#172b3f]/15 bg-white/70">{sound ? <Volume2 className="size-[17px]" /> : <VolumeX className="size-[17px]" />}</button><button onClick={toggleTheme} className="mq-theme-switch grid size-10 place-items-center rounded-full border border-[#172b3f]/15 bg-white/70">{theme === "light" ? <Moon className="size-[17px]" /> : <Sun className="size-[18px]" />}</button><Link href="/#path" className="mq-library-link inline-flex items-center gap-2 rounded-full border border-[#172b3f]/15 px-3 py-2 text-sm font-extrabold"><ArrowLeft className="size-4" /><span>返回題目庫</span></Link></div></div></header><main className="mx-auto max-w-[1280px] px-5 py-8 lg:px-8 lg:py-10"><div className="mq-route-ruler"><span>起點</span><i /><span>{info.code}</span><i /><span>互動站</span></div><div className="mb-6 flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="font-mono text-[11px] font-bold tracking-[.16em] text-[#f05a3c]">P1 · 探索幾何與量度</p><h1 className="mt-2 text-3xl font-black tracking-[-.055em] sm:text-4xl">{info.title}</h1><p className="mt-2 text-sm leading-6 text-[#617286]">{info.subtitle}</p></div><div className="mq-progress-label rounded-2xl border border-[#172b3f]/10 bg-white px-4 py-3 text-sm"><span className="font-mono text-[10px] font-bold tracking-[.12em] text-[#f05a3c]">探索任務</span><p className="mt-1 font-extrabold">完成 {completed} / 8 題 · {timeLabel}</p></div></div><div className="mq-shape-controls"><div><span className="mq-station-stamp">{mode === "solid" ? "SOLID SORT" : mode === "rotate" ? "TURN LAB" : "SORT LAB"}</span><strong>{info.action}</strong><small>{info.hint}</small></div></div><div className="mq-practice-grid mt-5 grid gap-5 lg:grid-cols-[230px_minmax(0,1fr)]"><aside className="mq-practice-sidebar order-2 lg:order-1"><div className="rounded-[24px] border border-[#172b3f]/10 bg-[#172b3f] p-5 text-white shadow-[0_10px_0_#0e1d2a]"><p className="font-mono text-[10px] font-bold tracking-[.16em] text-[#f6be5d]">探索任務</p><h2 className="mt-3 text-xl font-black leading-7">先看，再動手試。</h2><div className="mq-mission-steps mt-5">{Array.from({ length: 8 }, (_, i) => <span key={i} className={i < completed ? "mq-mission-step-done" : i === index ? "mq-mission-step-current" : ""}>{i + 1}</span>)}</div><div className="mt-6 h-2 overflow-hidden rounded-full bg-white/15"><div className="h-full rounded-full bg-[#f05a3c] transition-[width] duration-500" style={{ width: `${completed / 8 * 100}%` }} /></div><div className="mq-stars mt-5">{[1,2,3].map((s) => <Star key={s} className={`size-5 ${s <= stars ? "fill-[#f6be5d] text-[#f6be5d]" : "text-white/20"}`} />)}</div></div><div className="mq-tip mt-5 rounded-2xl border border-[#172b3f]/10 bg-[#fff3e8] p-4"><div className="flex items-center gap-2 text-[#f05a3c]"><Lightbulb className="size-4" /><span className="font-mono text-[10px] font-bold tracking-[.12em]">小提示</span></div><p className="mt-2 text-sm font-bold leading-6 text-[#744230]">{info.hint}</p></div></aside><section className="mq-practice-card order-1 min-h-[560px] overflow-hidden rounded-[28px] border border-[#172b3f]/10 bg-white p-5 shadow-[0_16px_35px_rgba(23,43,63,.07)] md:p-8">{result === "correct" && <CorrectCelebration />}{!finished ? <><div className="mq-question-head flex items-center justify-between border-b border-[#172b3f]/10 pb-5"><div><p className="font-mono text-[10px] font-bold tracking-[.14em] text-[#f05a3c]">探索站 · 第 {index + 1} 題</p><p className="mt-1 text-sm font-bold text-[#617286]">{info.action}</p></div><span className="grid size-10 place-items-center rounded-full bg-[#fff0e9] font-mono text-xs font-black text-[#f05a3c]">{index + 1}</span></div><div className="flex min-h-[345px] flex-col items-center justify-center py-7 text-center"><div className="mq-question-note"><span className="font-mono">動手提示</span><span>先看清楚，再按答案。</span></div><SpeakButton text={prompt} /><h2 className="mt-3 text-[clamp(1.5rem,4vw,2.4rem)] font-black tracking-[-.05em]">{mode === "solid" ? <>這個 <b className="text-[#f05a3c]">{(item as typeof solidItems[number]).name}</b> 是甚麼立體形狀？</> : mode === "rotate" ? <>把圖形轉到 <b className="text-[#f05a3c]">箭嘴方向</b></> : <>由 <b className="text-[#f05a3c]">短到長</b> 點選物件</>}</h2>{mode === "solid" && <div className="mq-solid-board"><div className="mq-life-object">{(item as typeof solidItems[number]).emoji}<span>{(item as typeof solidItems[number]).name}</span></div><div className="mq-solid-choice-grid">{(["球體", "柱體", "錐體"] as Solid[]).map((shape) => <button key={shape} onClick={() => verify(shape)} className={`mq-solid-choice ${result === "correct" && shape === (item as typeof solidItems[number]).answer ? "is-correct" : result === "incorrect" ? "is-wrong" : ""}`}><Cuboid className="size-8" /><span>{shape}</span></button>)}</div></div>}{mode === "rotate" && <div className="mq-rotate-board"><div className="mq-rotation-target">目標方向 <span style={{ transform: `rotate(${(item as typeof rotationItems[number]).degree}deg)` }}>➤</span></div><button onClick={() => setRotation((v) => (v + 45) % 360)} className="mq-rotating-shape" style={{ transform: `rotate(${rotation}deg)` }}><span>{(item as typeof rotationItems[number]).shape}</span><small>按我轉 45°</small></button><button onClick={() => verify()} className="mq-start rounded-full bg-[#f05a3c] px-5 py-3 text-sm font-extrabold text-white">檢查方向</button></div>}{mode === "sort" && <div className="mq-sort-board"><div className="mq-sort-tray">{shuffle(item as typeof sortItems[number]).map((entry) => <button key={entry.name} onClick={() => setSelected((items) => items.includes(entry.name) ? items : [...items, entry.name])} className={`mq-sort-item ${selected.includes(entry.name) ? "is-picked" : ""}`}><span>{entry.emoji}</span><strong>{entry.name}</strong><small>{entry.length} 厘米</small></button>)}</div><div className="mq-sort-order">{[0,1,2].map((slot) => <span key={slot}>{selected[slot] ? `${slot + 1}. ${selected[slot]}` : `${slot + 1}. ?`}</span>)}</div><button onClick={() => verify()} disabled={selected.length !== 3} className="mq-start rounded-full bg-[#f05a3c] px-5 py-3 text-sm font-extrabold text-white disabled:opacity-40">檢查排序</button></div>}</div>{result !== "idle" && <div className={`mq-feedback mb-5 flex items-start gap-3 rounded-2xl border p-4 ${result === "correct" ? "border-[#0e8b87]/25 bg-[#e8f5f2] text-[#135d59]" : "border-[#f05a3c]/25 bg-[#fff0e9] text-[#8c3f2e]"}`}><span className={`grid size-8 shrink-0 place-items-center rounded-full ${result === "correct" ? "bg-[#0e8b87]" : "bg-[#f05a3c]"} text-white`}>{result === "correct" ? <Check className="size-4" /> : <X className="size-4" />}</span><div><p className="font-extrabold">{result === "correct" ? "答對了！你做得很好。" : "再試一次，慢慢看提示。"}</p><p className="mt-1 text-sm opacity-80">{info.hint}</p></div></div>}<div className="flex justify-end">{result === "idle" ? <p className="rounded-2xl border border-dashed border-[#172b3f]/15 px-5 py-4 text-sm font-bold text-[#617286]">{mode === "sort" ? "由短到長，逐個按物件。" : "先動手試一試。"}</p> : result === "incorrect" ? <button onClick={resetQuestion} className="mq-start min-h-14 rounded-2xl bg-[#f05a3c] px-6 text-sm font-extrabold text-white"><RotateCcw className="mr-1 inline size-4" /> 再試一次</button> : <button onClick={next} className="mq-start min-h-14 rounded-2xl bg-[#f05a3c] px-6 text-sm font-extrabold text-white">{index === 7 ? "看結果" : "下一題"} <ArrowRight className="ml-1 inline size-4" /></button>}</div></> : <div className="flex min-h-[500px] flex-col items-center justify-center text-center"><span className="grid size-20 place-items-center rounded-[26px] bg-[#fff0e9] text-[#f05a3c]"><Trophy className="size-10" /></span><p className="mt-7 font-mono text-[11px] font-bold tracking-[.16em] text-[#f05a3c]">完成探索站</p><h2 className="mt-3 text-3xl font-black tracking-[-.05em]">{info.title}完成了！</h2><div className="mq-finish-stars mt-4">{[1,2,3].map((s) => <Star key={s} className={`size-7 ${s <= stars ? "fill-[#f6be5d] text-[#f6be5d]" : "text-[#e8e3d9]"}`} />)}</div><p className="mt-3 text-[15px] leading-7 text-[#617286]">答對 {score} / 8 題。</p><div className="mt-7 flex flex-wrap justify-center gap-3"><button onClick={restart} className="mq-start inline-flex items-center gap-2 rounded-full bg-[#f05a3c] px-5 py-3 text-sm font-extrabold text-white"><RotateCcw className="size-4" /> 再做一次</button><Link href="/#path" className="mq-library-return inline-flex items-center gap-2 rounded-full border border-[#172b3f]/15 px-5 py-3 text-sm font-extrabold">返回題目庫 <ArrowRight className="size-4" /></Link></div></div>}</section></div></main></div>;
}
