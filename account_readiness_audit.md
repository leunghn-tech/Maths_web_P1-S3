# 公開帳戶、登入及學習資料就緒度檢視

**檢視日期：2026-08-27**
**範圍：公開註冊、學生／教師登入、密碼重設、學習進度同步、教師學生清單及約 1,000 個帳戶的使用情境。**

> **結論：現行版本適合小規模、受控試用，但不建議在沒有補強前公開開放並預期約 1,000 名學生自行註冊。**

## 已驗證可運作的部分

| 項目 | 現行實作 | 判定 |
| --- | --- | --- |
| 學生註冊 | 用戶名稱具唯一鍵；格式限制為 3–48 個小寫英文字母、數字或 `._-`。 | 可用 |
| 密碼儲存 | 密碼及恢復碼以各自隨機 salt 的 `scrypt` 雜湊儲存；資料庫不保存明文密碼。 | 可用 |
| 帳戶會話 | 簽名 JWT 以 HttpOnly cookie 優先保存，並以 `sessionVersion` 令密碼變更後的舊會話失效。 | 可用 |
| 權限分隔 | 學習資料 API 要求登入；教師學生清單及學生詳情要求 admin 角色。 | 可用 |
| 學習資料 | 完成紀錄、每日紀錄、錯題及釘選均以 `studentId` 隔離，並設有防重覆的唯一鍵及學生索引。 | 可用 |
| 基本同步 | 以 `syncRevision` 偵測兩部裝置同時更新；雲端資料與本機完成紀錄、每日紀錄、錯題會取聯集／較高錯題數。 | 可用但需補強 |

已執行登入、授權、會話、密碼雜湊、教師權限及學習摘要的 13 項相關自動測試，全部通過。近期生產日誌未見持續的登入或同步 API 失敗；出現的「Missing session cookie」是未登入瀏覽公開頁時的預期記錄。

## 公開約 1,000 個帳戶前的阻擋問題

| 優先級 | 發現 | 為何不宜公開 | 建議處理 |
| --- | --- | --- | --- |
| **嚴重** | 教師帳戶為固定 `admin`，其初始密碼由環境設定恢復；若仍使用先前的簡短公開密碼，任何人均可登入教師後台。 | 可讀取所有學生的帳戶名稱、進度、準確度及錯題摘要。 | 立即改用長而唯一的環境密碼；停止在公開頁面或說明中提供教師密碼；加入安全的教師密碼變更流程。 |
| **嚴重** | 學生密碼只要求最少 4 位英文字母或數字；公開登入、註冊及恢復碼驗證沒有伺服器端嘗試次數限制或節流。 | 易受猜密碼、批量註冊及重複請求影響。 | 最少 8–10 位並允許密碼管理器；對登入、註冊及恢復碼 API 加入 IP＋帳戶維度的節流、暫停及 CAPTCHA／電郵驗證。 |
| **高** | 教師清單一次性讀取全部學生、進度統計與錯題統計，再於瀏覽器搜尋排序。 | 1,000 名學生可能仍能顯示，但資料及紀錄增長後會拖慢後台和資料庫查詢；目前沒有真實負載測試結果。 | 改為伺服器端搜尋、排序及分頁（例如每頁 25–50 人），並提供總數與最近 7 日摘要的專用查詢。 |
| **高** | 同步衝突時，前端把雲端資料與本機資料合併顯示，但不會立刻自動重送合併後快照。 | 使用者需要再做一次新的動作才會把合併資料真正備份；釘選的排列以雲端優先，可能覆蓋另一部裝置的排列。 | 於衝突後進行一次有版本檢查的自動重試；或把同步 API 改為伺服器端合併的事件／操作紀錄模型。 |
| **中** | 進度 API 接受任意符合字數限制的 `practiceKey`、年級、標題與連結，沒有白名單。 | 已登入學生只能污染自己的資料，但教師統計可被假紀錄影響。 | 由伺服器維護有效課題鍵值白名單；拒絕不存在的課題及不屬於 P1–P6 的年級。 |
| **中** | 沒有帳戶停用、資料刪除、匯出或管理稽核記錄。 | 公開兒童學習服務需要處理帳戶生命週期及家長／用戶的資料要求。 | 實作學生帳戶刪除／匯出、教師存取紀錄及最短必要資料原則；公開前請按實際服務地與對象向合資格私隱／法律人士確認。 |

## 對「1,000 個帳戶」的實際建議

目前資料模型採用關聯式資料表及學生索引；每名學生的核心資料量很小，**資料表設計本身並非第一個瓶頸**。但我無法在未進行真實負載測試、未確認部署資源限制及未實裝上述防濫用機制前，保證 1,000 名公開帳戶或同時登入能穩定運作。

建議分三步推行：先以 20–50 名受邀測試者驗證登入、同步和教師清單；完成節流、強密碼及後台分頁後，擴展至 200 名；最後才以監控到的登入失敗率、同步衝突率、API 回應時間及資料庫錯誤率決定是否公開至約 1,000 人。這是營運建議，不是對任何平台免費方案容量的承諾；Manus 方案的確實容量、限制及支援範圍應向 [Manus Help](https://help.manus.im) 確認。

## 建議的最小公開上線門檻

1. **先更換教師密碼**，不要再以固定、短的公開密碼運作。
2. **在伺服器實作登入／註冊／恢復碼節流**，並把學生密碼政策提高至至少 8 位。
3. **改造教師清單為伺服器端分頁、搜尋與排序**，避免一次讀取全部學生資料。
4. **修正同步衝突後的自動重試與釘選合併**，並加入兩部裝置衝突的測試。
5. **先做受控試用和負載測試**；公開前確認平台容量與兒童個人資料責任。

## 直接檢視的程式位置

| 範圍 | 檔案 |
| --- | --- |
| API 輸入與權限 | `server/routers.ts`、`server/_core/trpc.ts` |
| 密碼及恢復碼 | `server/localAuth.ts`、`server/db.ts` |
| 資料表與索引 | `drizzle/schema.ts` |
| 雲端同步與衝突提示 | `client/src/components/CloudLearningSync.tsx`、`client/src/lib/localLearningSnapshot.ts` |
| 教師清單規模 | `client/src/pages/TeacherDashboard.tsx`、`server/db.ts` |
